// Site-wide settings you should fill in before publishing.

window.SITE_CONFIG = {
  // Shown at the top of the page.
  businessName: "Hao's of Raves Tickets",
  tagline: "Grab your tickets below — I'll confirm your order shortly!",

  // Your Instagram username (without the @). Used for the DM fallback button.
  instagramHandle: "h0wfarm",

  // Formspree form endpoint. Sign up free at https://formspree.io,
  // create a form, and paste its endpoint URL here.
  // It looks like: https://formspree.io/f/xxxxxxxx
  formspreeEndpoint: "https://formspree.io/f/mljrkydy",

  // OPTIONAL — visitor stats. Leave as "" and no tracking script loads at all.
  //
  // To turn it on: sign up free at https://www.goatcounter.com, pick a site
  // code, and put it here. If your code is "haoraves", enter "haoraves".
  // GoatCounter sets no cookies and collects no personal data, so it needs
  // no cookie banner. You'll see pageviews, referrers, and which events
  // people search for — useful for knowing what to stock next.
  //
  // It also records a "ticket-request-sent" event each time someone
  // successfully submits the form, so you can see how many visits convert.
  goatCounterCode: ""
};
